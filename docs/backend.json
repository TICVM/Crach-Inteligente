{
  "entities": {
    "Aluno": {
      "title": "Aluno",
      "type": "object",
      "description": "Representa um aluno registrado no sistema, contendo seus dados pessoais e de identificação para o crachá.",
      "properties": {
        "nome": {
          "type": "string",
          "description": "Nome completo do aluno."
        },
        "turma": {
          "type": "string",
          "description": "Turma ou classe do aluno."
        },
        "fotoUrl": {
          "type": "string",
          "description": "URL de dados (Base64) da imagem de perfil."
        },
        "customData": {
          "type": "object",
          "description": "Campos personalizados (Matrícula, Validade, etc.)."
        }
      },
      "required": [
        "nome",
        "turma",
        "fotoUrl"
      ]
    },
    "ConfiguracaoCracha": {
      "title": "ConfiguracaoCracha",
      "type": "object",
      "description": "Configurações globais de design do crachá.",
      "properties": {
        "fundoCrachaUrl": {
          "type": "string",
          "description": "URL da imagem de fundo."
        },
        "badgeStyle": {
          "type": "object",
          "description": "Estilos de posicionamento, cores e fontes."
        }
      }
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "/alunos/{alunoId}": {
      "schema": {
        "$ref": "#/backend/entities/Aluno"
      },
      "description": "Coleção de alunos. Acesso permitido para usuários autenticados."
    },
    "/configuracaoCracha/{configuracaoId}": {
      "schema": {
        "$ref": "#/backend/entities/ConfiguracaoCracha"
      },
      "description": "Configuração global do crachá. Leitura pública, escrita autenticada."
    }
  }
}